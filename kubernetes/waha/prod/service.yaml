apiVersion: v1
kind: Service
metadata:
  name: waha
  annotations:
    cloud.google.com/neg: '{"exposed_ports": {"3000":{}}}'
spec:
  selector:
    app: waha
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
