apiVersion: apps/v1
kind: Deployment
metadata:
  name: dify-plugin-connector
spec:
  template:
    spec:
      containers:
      - name: dify-plugin-connector
        env:
        - name: DB_PASS
          valueFrom:
            secretKeyRef:
              name: password
              key: db-password
