apiVersion: v1
kind: Service
metadata:
  name: ntfy
  annotations:
    cloud.google.com/neg: '{"exposed_ports": {"80":{}}}'
spec:
  selector:
    app: ntfy
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
