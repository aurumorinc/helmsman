apiVersion: v1
kind: Service
metadata:
  namespace: komga
  name: komga
  labels:
    app: komga
  annotations:
    cloud.google.com/neg: '{"exposed_ports": {"80":{}}}'
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: komga
  type: ClusterIP
